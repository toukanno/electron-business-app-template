{
  "name": "electron-business-app-template",
  "version": "1.0.0",
  "description": "会計帳簿作成ExcelソフトのElectronネイティブアプリ",
  "main": "src/main/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "test": "node --test",
    "lint": "node --check src/main/main.js && node --check src/main/preload.js && node --check src/main/db.js && node --check src/renderer/app.js"
  },
  "keywords": [
    "electron",
    "accounting",
    "ledger"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "electron": "^31.0.2"
  }
}
